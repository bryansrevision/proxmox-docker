# ==================================================================
# Proxmox Personal Data Platform - Docker Environment Config
# ==================================================================

# ------------------------------------------------------------------
# REQUIRED: Proxmox Connection Settings
# ------------------------------------------------------------------

# Proxmox server IP address or hostname
PROXMOX_HOST=192.168.1.185

# Proxmox API port (default: 8006)
PROXMOX_PORT=8006

# Proxmox user (format: username@realm)
# Common realms: pam (Linux), pve (Proxmox)
PROXMOX_USER=root@pam

# ------------------------------------------------------------------
# REQUIRED: Authentication (choose ONE)
# ------------------------------------------------------------------

# ALTERNATIVE: Password Authentication (from Bitwarden)
PROXMOX_PASSWORD=root

# ------------------------------------------------------------------
# Connection Options
# ------------------------------------------------------------------

# Verify SSL certificates (set false for self-signed certs)
PROXMOX_VERIFY_SSL=false

# API request timeout in seconds
PROXMOX_TIMEOUT=30

# ------------------------------------------------------------------
# SSH Configuration (Optional)
# ------------------------------------------------------------------

# SSH host (usually same as PROXMOX_HOST)
PROXMOX_SSH_HOST=192.168.1.185

# SSH port (default: 22)
PROXMOX_SSH_PORT=22

# SSH user (default: root)
PROXMOX_SSH_USER=root

# SSH key path (leave empty for ~/.ssh/id_rsa)
PROXMOX_SSH_KEY_PATH=/home/pxmgr/.ssh/id_rsa

# ------------------------------------------------------------------
# Docker Specific Options
# ------------------------------------------------------------------

# Configuration directory inside container
CONFIG_DIR=/app/config

# Logs directory inside container
LOG_DIR=/app/logs

# Application logging level (DEBUG, INFO, WARNING, ERROR)
PXMGR_LOG_LEVEL=INFO

# Enable dry-run mode by default (true/false)
PXMGR_DRY_RUN=false

# Default output format (table, json, yaml)
PXMGR_OUTPUT_FORMAT=table

# ------------------------------------------------------------------
# Examples for Different Setups
# ------------------------------------------------------------------

# Home Lab (self-signed certificate):
#   PROXMOX_HOST=192.168.1.100
#   PROXMOX_USER=root@pam
#   PROXMOX_TOKEN_NAME=homelab
#   PROXMOX_TOKEN_VALUE=token-uuid-here
#   PROXMOX_VERIFY_SSL=false

# Production (valid certificate):
#   PROXMOX_HOST=pve.company.com
#   PROXMOX_USER=automation@pve
#   PROXMOX_TOKEN_NAME=automation
#   PROXMOX_TOKEN_VALUE=token-uuid-here
#   PROXMOX_VERIFY_SSL=true

# Development (password auth):
#   PROXMOX_HOST=dev-pve.local
#   PROXMOX_USER=root@pam
#   PROXMOX_PASSWORD=password123
#   PROXMOX_VERIFY_SSL=false
